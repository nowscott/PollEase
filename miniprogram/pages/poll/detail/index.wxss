/* pages/poll/detail/index.wxss */

.page-wrapper {
  background-color: var(--notion-bg);
  min-height: 100vh;
  color: var(--notion-text);
}

.container {
  min-height: 100vh;
  box-sizing: border-box;
  display: flex;
  flex-direction: column;
}

/* 标题区域样式 */
.poll-header {
  margin: 0;
  padding: 60rpx 48rpx 48rpx;
  background: var(--notion);
  width: 100%;
  box-sizing: border-box;
}

.poll-title {
  font-size: 48rpx;
  font-weight: 600;
  color: var(--notion-text);
  margin-bottom: 24rpx;
  line-height: 1.3;
  letter-spacing: -0.02em;
}

.poll-meta {
  font-size: 24rpx;
  color: var(--notion-text-light);
  display: flex;
  flex-direction: column;
  gap: 8rpx;
}

/* 投票内容区域 */
.poll-content {
  background: var(--notion-b g);
  margin: 0;
  width: 100%;
  padding: 24rpx 0;
  flex: 1;
}

/* 投票状态标签 */
.poll-status {
  margin: 0 0 24rpx;
  padding: 0 48rpx;
  display: flex;
  justify-content: flex-start;
}

.expired-tag, .voted-tag {
  display: inline-flex;
  align-items: center;
  padding: 4rpx 12rpx;
  border: 2rpx solid var(--notion-border);
  border-radius: 8rpx;
  font-size: 24rpx;
  font-weight: 500;
  background: var(--color-warning);
  color: var(--notion-text-light);
}

/* 选项样式 */
.poll-options {
  display: flex;
  flex-direction: column;
  gap: 12rpx;
  padding: 0 48rpx;
}

.option {
  background: var(--notion-bg);
  padding: 24rpx;
  border: 2rpx solid var(--notion-border);
  border-radius: 20rpx;
  position: relative;
  overflow: hidden;
  transition: all 0.2s ease;
  margin-bottom: 12rpx;
  box-sizing: border-box;
}

.option.can-vote:active {
  background: var(--notion-hover);
}

.option-content {
  display: flex;
  justify-content: space-between;
  align-items: center;
  position: relative;
  z-index: 1;
  padding: 0 24rpx;
  width: 100%;
}

.option-text {
  font-size: 28rpx;
  color: var(--notion-text);
  flex: 1;
  margin-right: 32rpx;
  line-height: 1.5;
}

.vote-info {
  display: flex;
  flex-direction: row;
  align-items: center;
  gap: 16rpx;
  min-width: max-content;
  padding: 0 24rpx;
}

.vote-count {
  font-size: 28rpx;
  color: var(--notion-text);
  font-weight: 400;
}

.vote-percentage {
  font-size: 28rpx;
  color: var(--notion-text-light);
  font-weight: 400;
}

/* 加载和错误状态 */
.loading, .error {
  text-align: center;
  padding: 60rpx 40rpx;
  color: var(--notion-text-light);
  font-size: 28rpx;
}

.error {
  color: #666;
}

.retry-btn {
  margin-top: 24rpx;
  background: var(--notion-bg);
  border: 2rpx solid var(--notion-border);
  color: var(--notion-text);
  font-size: 28rpx;
  padding: 12rpx 24rpx;
  border-radius: 8rpx;
}

/* 分享按钮 */
.share-section {
  padding: 24rpx 32rpx 48rpx;
  background: var(--notion-bg);
}

.share-btn {
  width: calc(100% - 64rpx);
  margin: 0 auto;
  height: 80rpx;
  line-height: 80rpx;
  border-radius: 20rpx;
  background: var(--color-warning);
  color: var(--notion-text);
  font-size: 28rpx;
  font-weight: 500;
  border: 2rpx solid var(--notion-border);
  transition: all 0.2s ease;
  display: flex;
  align-items: center;
  justify-content: center;
}

.share-btn::after {
  border: none;
}

.share-btn:active {
  background: var(--color-warning-hover);
}